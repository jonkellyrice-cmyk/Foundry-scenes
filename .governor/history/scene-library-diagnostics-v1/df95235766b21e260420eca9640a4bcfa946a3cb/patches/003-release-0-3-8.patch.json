{
  "schemaVersion": 1,
  "id": "release-0-3-8",
  "phase": "release-0-3-8",
  "summary": "Publish the Scene Library Diagnostics tab and telemetry tooling as module version 0.3.8 without changing the Ghost Ship scene-template version.",
  "operations": [
    {
      "op": "json-set",
      "path": "module.json",
      "keyPath": ["version"],
      "expected": "0.3.7",
      "value": "0.3.8"
    }
  ],
  "checks": [
    {
      "name": "Manifest version",
      "run": "node -e \"const m=JSON.parse(require('fs').readFileSync('module.json','utf8')); if(m.version!=='0.3.8') throw new Error('expected 0.3.8');\""
    },
    {
      "name": "Diagnostics script is packaged by existing recursive scripts copy",
      "run": "test -s scripts/scene-diagnostics.mjs && grep -q 'cp -R assets scripts styles templates' .github/workflows/release.yml"
    }
  ]
}
